import { Html } from "@react-three/drei";

export default function Loading3DModel() {
return (
        <Html center>
            <div className="text-white text-lg animate-pulse">🌌 Loading 3D Model...</div>
        </Html>
    );
}
